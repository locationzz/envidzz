# dependencies
node_modules/

# serverless function source (deployed to Vercel, never committed — holds the Supabase key)
/api/

# os
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# project guide — kept local only, never pushed to GitHub
CLAUDE.md
